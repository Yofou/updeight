{
  "name": "updeight",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "start:backend": "turbo run build --filter=backend && cd ./packages/prisma && npm run migrate && cd ../../apps/backend && npm run start:prod",
    "start:frontend": "turbo run build --filter=frontend && cd ./apps/frontend && npm run start:prod",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint-config-custom": "workspace:*",
    "prettier": "latest",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {},
  "packageManager": "pnpm@7.7.0"
}
